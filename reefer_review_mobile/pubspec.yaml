name: reefer_review_mobile
description: A new Flutter project.
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.0.6 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  loading_indicator: ^3.1.1
  expandable_text: ^2.1.0
  meta: ^1.3.0
  bloc:
  flutter_bloc:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  fonts:
    - family: Ranchers
      fonts:
        - asset: assets/fonts/Ranchers-Regular.ttf

  assets:
    - "assets/images/"
    - "assets/fonts/"
    - "assets/files/"
